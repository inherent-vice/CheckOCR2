# Legacy Application Snapshots

This directory keeps historical Check Capture OCR variants that were previously
stored at the repository root. They are preserved for comparison and recovery
only.

Use `../check_capture_ocr.py` for current development and packaging. The legacy
files may not match the current build scripts or documentation.
