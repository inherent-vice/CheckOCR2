# Development Tools

This directory contains one-off utility scripts, mainly icon generation helpers.
They are not part of the runtime application.

Keep generated runtime assets, such as `.ico` and `.png` files used by the
PyInstaller specs, at the repository root unless the build scripts are updated
at the same time.
